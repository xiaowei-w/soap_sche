extends layout

block content
  div.container
    div.row
      h1.page-header= title
    
    div.row
      button.btn.btn-default.btn-sm(onClick="initAddRow()")
        span(aria-hidden="true") Add Resource

      button.btn.btn-default.btn-sm(onClick="initDelRow()")
        span(aria-hidden="true") Delete Resource
    br
    div.row
      #dp